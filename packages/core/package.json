{
  "name": "react-native-zephyr",
  "author": "Formidable",
  "description": "Styling library for React Native",
  "keywords": [
    "react",
    "react-native",
    "styling"
  ],
  "version": "0.1.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/formidablelabs/smol-range.git"
  },
  "exports": {
    ".": [
      {
        "imports": "./dist/index.js",
        "types": "./dist/index.d.ts"
      },
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p ./tsconfig.build.json",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepublishOnly": "cp ../../README.md ./README.md && yarn test && yarn build",
    "preversion": "yarn run test"
  },
  "peerDependencies": {
    "react": ">=16.0.0",
    "react-native": ">=0.66.4"
  }
}
